version: '3.9'

services:
  postgres:
    image: postgres:16-alpine
    container_name: sportlink-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: sportlink
      POSTGRES_PASSWORD: sportlink
      POSTGRES_DB: sportlink
    ports:
      - '5432:5432'
    volumes:
      - sportlink_pgdata:/var/lib/postgresql/data

volumes:
  sportlink_pgdata:
